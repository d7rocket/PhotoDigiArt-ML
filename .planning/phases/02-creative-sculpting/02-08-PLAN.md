---
phase: 02-creative-sculpting
plan: 08
type: execute
wave: 2
depends_on: ["02-07"]
files_modified:
  - tests/test_simulation_params.py
  - tests/test_sim_lifecycle.py
autonomous: true
gap_closure: true
requirements: [EXTRACT-05, RENDER-05, RENDER-06, SIM-01, SIM-04, CTRL-01, CTRL-03, CTRL-04, CTRL-05, CTRL-06]

must_haves:
  truths:
    - "All tests pass with the current all-visual-params behavior"
    - "Test suite reflects the deliberate design decision from commit d2f401c"
    - "No stale test expectations remain"
  artifacts:
    - path: "tests/test_simulation_params.py"
      provides: "Updated param classification tests matching all-visual behavior"
      contains: "is_visual_param"
    - path: "tests/test_sim_lifecycle.py"
      provides: "Updated routing tests matching all-visual behavior"
      contains: "visual_calls"
  key_links:
    - from: "tests/test_simulation_params.py"
      to: "apollo7/simulation/parameters.py"
      via: "is_visual_param and is_physics_param classification"
      pattern: "is_visual_param|is_physics_param"
    - from: "tests/test_sim_lifecycle.py"
      to: "apollo7/gui/widgets/viewport_widget.py"
      via: "update_sim_param routing to update_visual_param"
      pattern: "update_sim_param|_visual_calls"
---

<objective>
Fix 4 stale tests that expect the old physics/visual parameter distinction.

Purpose: Commit d2f401c deliberately reclassified all simulation params as visual (hot-reload without restart). Four tests still expect the old classification where gravity, wind, viscosity, etc. were physics params requiring sim restart. These tests fail against the current (correct, user-approved) behavior. This plan updates the test expectations to match reality.
Output: All tests green with correct expectations.
</objective>

<execution_context>
@C:/Users/DeveshD/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DeveshD/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-creative-sculpting/02-VERIFICATION.md

Key source files:
@tests/test_simulation_params.py
@tests/test_sim_lifecycle.py
@apollo7/simulation/parameters.py

<interfaces>
From apollo7/simulation/parameters.py (current behavior post d2f401c):
```python
@classmethod
def is_visual_param(cls, name: str) -> bool:
    """All tunable params are visual (hot-reload, no restart needed)."""
    return name in {
        "noise_frequency", "noise_amplitude", "noise_octaves", "turbulence_scale",
        "speed", "damping", "gravity", "wind",
        "viscosity", "pressure_strength", "surface_tension", "attraction_strength",
        "repulsion_strength", "repulsion_radius", "smoothing_radius", "rest_density",
        "gas_constant",
    }

@classmethod
def is_physics_param(cls, name: str) -> bool:
    """No params require restart -- all are visual."""
    return False
```

From apollo7/gui/widgets/viewport_widget.py update_sim_param routing:
All params route to update_visual_param (since is_physics_param always returns False).
Compound params (gravity_y, wind_x) also route to update_visual_param with the tuple.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update stale test expectations for all-visual param classification</name>
  <files>tests/test_simulation_params.py, tests/test_sim_lifecycle.py</files>
  <action>
Fix 4 failing tests to match the current all-visual-params behavior (deliberately changed in commit d2f401c and user-approved):

1. **tests/test_simulation_params.py::TestParamClassification::test_physics_params** (line 117-137):
   This test asserts that viscosity, pressure_strength, surface_tension, attraction_strength, repulsion_strength, repulsion_radius, smoothing_radius, rest_density, gas_constant, gravity, and wind are physics params. Since d2f401c, ALL params are visual. Fix by:
   - Change the test to assert these are all VISUAL params (is_visual_param returns True)
   - Assert is_physics_param returns False for all of them
   - Rename the test method to `test_all_params_are_visual` to clearly document the design decision
   - Add a comment: "# All params reclassified as visual in d2f401c -- hot-reload without restart"

2. **tests/test_sim_lifecycle.py::TestSimLifecycle::test_update_sim_param_routes_physics** (line 252-261):
   This test asserts viscosity routes to update_physics_param (engine._physics_calls). Since all params are now visual, fix by:
   - Change to assert viscosity routes to update_visual_param (engine._visual_calls)
   - Rename to `test_update_sim_param_viscosity_routes_visual`
   - Assert _physics_calls is empty

3. **tests/test_sim_lifecycle.py::TestSimLifecycle::test_update_sim_param_gravity_y** (line 263-276):
   This test asserts gravity_y routes to _physics_calls. Fix by:
   - Change to assert gravity routes to _visual_calls
   - Rename to reflect visual routing
   - Verify the gravity tuple is correctly formed with the updated y component

4. **tests/test_sim_lifecycle.py::TestSimLifecycle::test_update_sim_param_wind_x** (line 278-290):
   This test asserts wind_x routes to _physics_calls. Fix by:
   - Change to assert wind routes to _visual_calls
   - Rename to reflect visual routing
   - Verify the wind tuple is correctly formed with the updated x component

Run `pytest tests/test_simulation_params.py tests/test_sim_lifecycle.py -x --timeout=30` after changes to confirm all pass.
  </action>
  <verify>
    <automated>pytest tests/test_simulation_params.py tests/test_sim_lifecycle.py -x --timeout=30</automated>
  </verify>
  <done>All 4 previously failing tests updated to match the all-visual-params behavior. Full test suite passes. No stale test expectations remain.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_simulation_params.py -x --timeout=30` passes (all param classification tests correct)
- `pytest tests/test_sim_lifecycle.py -x --timeout=30` passes (all routing tests correct)
- `pytest tests/ -x --timeout=30` full suite passes
</verification>

<success_criteria>
- test_physics_params updated to test_all_params_are_visual asserting is_visual_param returns True for all
- 3 lifecycle routing tests updated to check _visual_calls instead of _physics_calls
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/02-creative-sculpting/02-08-SUMMARY.md`
</output>
