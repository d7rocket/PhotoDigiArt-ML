---
phase: 04-stable-physics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apollo7/simulation/parameters.py
  - apollo7/simulation/buffers.py
  - tests/test_simulation_params.py
  - tests/test_pbf_solver.py
autonomous: true
requirements: [PHYS-01, PHYS-04, PHYS-08, PHYS-09]

must_haves:
  truths:
    - "SimulationParams contains all PBF + home + breathing fields with correct defaults"
    - "ParticleBuffer allocates home_positions, predicted_positions, lambda, delta_p buffers"
    - "Old SPH params (gas_constant, viscosity, pressure_strength, surface_tension, attraction_strength, repulsion_strength, repulsion_radius, smoothing_radius) are removed"
    - "Uniform buffer packs to 128 bytes matching new WGSL layout"
    - "Test scaffolds exist for all PHYS requirements"
  artifacts:
    - path: "apollo7/simulation/parameters.py"
      provides: "PBF simulation parameters with 8 x vec4 uniform layout"
      contains: "home_strength"
    - path: "apollo7/simulation/buffers.py"
      provides: "Extended particle buffer with PBF-specific GPU buffers"
      contains: "home_positions"
    - path: "tests/test_pbf_solver.py"
      provides: "Test scaffolds for PHYS-01 through PHYS-09"
      contains: "test_home_attraction_holds_form"
    - path: "tests/test_simulation_params.py"
      provides: "Updated param tests for PBF fields"
      contains: "home_strength"
  key_links:
    - from: "apollo7/simulation/parameters.py"
      to: "apollo7/simulation/buffers.py"
      via: "UNIFORM_SIZE and to_uniform_bytes()"
      pattern: "UNIFORM_SIZE.*128"
---

<objective>
Replace SPH simulation parameters with PBF parameters and extend ParticleBuffer with PBF-specific GPU buffers. Create test scaffolds for all Phase 4 requirements.

Purpose: Establish the data layer contracts (parameters, buffers, types) that all subsequent PBF plans build against. Without correct uniform layout and buffer allocation, no shader can be written.
Output: Updated parameters.py, buffers.py, and test scaffold files ready for PBF implementation.
</objective>

<execution_context>
@C:/Users/DeveshD/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DeveshD/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-stable-physics/04-CONTEXT.md
@.planning/phases/04-stable-physics/04-RESEARCH.md

<interfaces>
<!-- Existing interfaces the executor needs -->

From apollo7/simulation/parameters.py:
```python
@dataclass
class SimulationParams:
    # Current: 7 x vec4 = 112 bytes, SPH params
    # Target: 8 x vec4 = 128 bytes, PBF params
    UNIFORM_SIZE: int = 112  # Must become 128
    def to_uniform_bytes(self) -> bytes: ...
    def with_update(self, **kwargs) -> SimulationParams: ...
    @classmethod
    def is_visual_param(cls, name: str) -> bool: ...
```

From apollo7/simulation/buffers.py:
```python
class ParticleBuffer:
    def __init__(self, device, max_particles: int): ...
    def upload(self, positions: np.ndarray, colors: np.ndarray) -> None: ...
    def build_spatial_hash(self, positions: np.ndarray, cell_size: float = 0.2) -> None: ...
    def swap(self) -> None: ...
    # Properties: input_buffer, output_buffer, color_buffer, params_buffer,
    #   forces_buffer, sph_forces_buffer, densities_buffer,
    #   cell_counts_buffer, cell_offsets_buffer, sorted_indices_buffer
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace SPH params with PBF params and extend uniform layout</name>
  <files>apollo7/simulation/parameters.py, tests/test_simulation_params.py</files>
  <behavior>
    - Test: SimulationParams has fields home_strength (default 5.0), breathing_rate (default 0.2), breathing_amplitude (default 0.15), kernel_radius (default 0.1), rest_density (default 6378.0), epsilon_pbf (default 600.0), solver_iterations (default 2), artificial_pressure_k (default 0.0001), artificial_pressure_n (default 4), delta_q (default 0.03), xsph_c (default 0.01), vorticity_epsilon (default 0.01), max_force (default 50.0), max_velocity (default 10.0)
    - Test: Old SPH-only fields removed: gas_constant, viscosity, pressure_strength, surface_tension, attraction_strength, repulsion_strength, repulsion_radius, smoothing_radius
    - Test: UNIFORM_SIZE is 128 (8 x vec4)
    - Test: to_uniform_bytes() produces exactly 128 bytes
    - Test: home_strength packs at vec4 1 offset 16 (bytes 16-19)
    - Test: kernel_radius packs at vec4 2 offset 32 (bytes 32-35)
    - Test: vorticity_epsilon packs at vec4 4 offset 64 (bytes 64-67)
    - Test: gravity still packs correctly at vec4 5 offset 80
    - Test: time packs at vec4 7 offset 112 (bytes 112-115)
    - Test: with_update works for new fields (home_strength, solver_iterations)
    - Test: all new PBF params are classified as visual (hot-reload, no restart)
    - Test: breathing_modulation method returns value in [0.85, 1.15] range for breathing_amplitude=0.15
  </behavior>
  <action>
    Rewrite SimulationParams to replace SPH parameters with PBF parameters. The new WGSL-aligned uniform layout (8 x vec4 = 128 bytes):

    vec4 0: noise_frequency, noise_amplitude, noise_octaves(as float), turbulence_scale
    vec4 1: home_strength, breathing_rate, breathing_amplitude, breathing_mod (computed per-frame)
    vec4 2: kernel_radius, rest_density, epsilon_pbf, solver_iterations (as float)
    vec4 3: artificial_pressure_k, artificial_pressure_n, delta_q, xsph_c
    vec4 4: vorticity_epsilon, max_force, max_velocity, dt
    vec4 5: gravity.xyz, damping
    vec4 6: wind.xyz, speed
    vec4 7: time, cell_size (= kernel_radius), particle_count, _pad

    Remove fields: gas_constant, viscosity, pressure_strength, surface_tension, attraction_strength, repulsion_strength, repulsion_radius, smoothing_radius, sph_enabled, performance_mode, attractor_global_strength.

    Add fields with defaults from research: home_strength=5.0, breathing_rate=0.2, breathing_amplitude=0.15, kernel_radius=0.1, rest_density=6378.0, epsilon_pbf=600.0, solver_iterations=2, artificial_pressure_k=0.0001, artificial_pressure_n=4, delta_q=0.03, xsph_c=0.01, vorticity_epsilon=0.01, max_force=50.0, max_velocity=10.0.

    Keep fields: noise_frequency, noise_amplitude, noise_octaves, turbulence_scale, speed, dt, damping, gravity, wind, time.

    Add breathing_mod field (runtime, not user-tunable, default 1.0).
    Add particle_count field (runtime, int, default 0).

    Add method compute_breathing(time: float) -> float that returns 1.0 + breathing_amplitude * sin(2 * pi * breathing_rate * time). This is called per-frame by the engine to update breathing_mod before uploading uniforms.

    Update _visual_params set to include all new PBF params. Update is_visual_param. Keep is_physics_param returning False (all params hot-reload).

    Update to_uniform_bytes() to pack the new 128-byte layout. Update UNIFORM_SIZE to 128.

    Update tests/test_simulation_params.py: remove tests for old SPH params, add tests for new PBF params, update uniform byte offset tests, add breathing_modulation test. Keep TestWithUpdate and TestParticleBufferWithMock classes (update param references if needed).
  </action>
  <verify>
    <automated>python -m pytest tests/test_simulation_params.py -x -q</automated>
  </verify>
  <done>SimulationParams has all PBF fields, 128-byte uniform layout, breathing computation, no SPH remnants. All param tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Extend ParticleBuffer with PBF buffers and create test scaffolds</name>
  <files>apollo7/simulation/buffers.py, tests/test_pbf_solver.py</files>
  <action>
    Extend ParticleBuffer.__init__ to allocate four new GPU buffers alongside existing ones:

    1. home_positions buffer: vec4 per particle (xyz = home pos, w = feature_strength). Usage: STORAGE | COPY_DST. Size: max_particles * 16.
    2. predicted_positions buffer: vec4 per particle (PBF predicted pos). Usage: STORAGE | COPY_DST. Size: max_particles * 16.
    3. lambda buffer: f32 per particle (constraint multiplier). Usage: STORAGE | COPY_DST. Size: max_particles * 4.
    4. delta_p buffer: vec4 per particle (position corrections). Usage: STORAGE | COPY_DST. Size: max_particles * 16.

    Add properties: home_positions_buffer, predicted_buffer, lambda_buffer, delta_p_buffer.

    Modify upload() method: after uploading particle state, also compute and upload home_positions. Home positions are a copy of the initial positions with w=1.0 (default feature strength; feature modulation will be set by the engine when feature textures are available).

    Keep existing buffers (forces_buf, sph_forces_buf, densities_buf, cell_counts_buf, cell_offsets_buf, sorted_indices_buf) -- they are reused by PBF. The sph_forces_buf can be repurposed or left (PBF does not use it but it does no harm).

    Update UNIFORM_SIZE reference: the ParticleBuffer creates params_buf with SimulationParams.UNIFORM_SIZE. Since Task 1 changes UNIFORM_SIZE to 128, this will automatically work. But verify the import still works.

    Create tests/test_pbf_solver.py with test STUBS (not full implementations yet -- they will be fleshed out as the solver is built). Each stub should use pytest.mark.skip(reason="PBF solver not yet implemented") so the test suite stays green. Include stubs for:
    - test_home_attraction_holds_form (PHYS-01)
    - test_stability_1000_frames (PHYS-02)
    - test_gpu_spatial_hash_correctness (PHYS-03)
    - test_no_nan_inf_after_1000_frames (PHYS-04)
    - test_cfl_timestep_adapts (PHYS-05)
    - test_curl_noise_produces_flow (PHYS-06)
    - test_vorticity_confinement_effect (PHYS-07)
    - test_breathing_modulation (PHYS-08)
    - test_iteration_count_affects_density (PHYS-09)

    Each stub should have a docstring describing what it will test and which requirement it covers.
  </action>
  <verify>
    <automated>python -m pytest tests/test_simulation_params.py tests/test_pbf_solver.py -x -q</automated>
  </verify>
  <done>ParticleBuffer has home_positions, predicted, lambda, delta_p buffers with properties. test_pbf_solver.py has 9 skipped stubs covering PHYS-01 through PHYS-09. Full test suite passes (stubs skipped, existing tests green).</done>
</task>

</tasks>

<verification>
python -m pytest tests/test_simulation_params.py tests/test_pbf_solver.py -x -q
</verification>

<success_criteria>
- SimulationParams has 128-byte uniform layout with all PBF fields
- Old SPH params completely removed
- ParticleBuffer allocates 4 new PBF-specific GPU buffers
- Home positions uploaded alongside particle state
- Test stubs exist for all 9 PHYS requirements
- All tests pass (new params tests green, PBF stubs skipped)
</success_criteria>

<output>
After completion, create `.planning/phases/04-stable-physics/04-01-SUMMARY.md`
</output>
