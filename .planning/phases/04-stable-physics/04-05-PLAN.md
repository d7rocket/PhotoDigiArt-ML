---
phase: 04-stable-physics
plan: 05
type: execute
wave: 5
depends_on: ["04-04"]
files_modified:
  - apollo7/gui/panels/sim_panel.py
  - apollo7/gui/main_window.py
  - tests/test_pbf_solver.py
  - tests/test_sim_panel.py
autonomous: false
requirements: [PHYS-09]

must_haves:
  truths:
    - "User can adjust solver iterations slider and see simulation character change in real-time"
    - "Slider labeled 'Cohesion' with 'Ethereal' to 'Liquid' spectrum"
    - "1 iteration = wispy/gas, 2 = default fluid, 4+ = dense/liquid, 6 = near-solid"
    - "Iteration changes crossfade smoothly over ~0.5s via parameter interpolation"
  artifacts:
    - path: "apollo7/gui/panels/sim_panel.py"
      provides: "Updated simulation panel with PBF controls"
      contains: "Cohesion"
    - path: "tests/test_pbf_solver.py"
      provides: "PHYS-09 test un-skipped and passing"
      contains: "test_iteration_count_affects_density"
  key_links:
    - from: "apollo7/gui/panels/sim_panel.py"
      to: "apollo7/simulation/engine.py"
      via: "update_visual_param for solver_iterations, home_strength, breathing_rate"
      pattern: "update_visual_param"
---

<objective>
Wire PBF parameters to GUI controls and verify the full physics pipeline end-to-end with a visual checkpoint. Make solver iterations a creative control with intuitive labeling.

Purpose: Connect the PBF simulation to user-facing controls so the sculpture can be interactively tuned. Verify the complete "alive" aesthetic visually.
Output: Working GUI controls for PBF params, visual verification of organic particle behavior.
</objective>

<execution_context>
@C:/Users/DeveshD/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DeveshD/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-stable-physics/04-CONTEXT.md
@.planning/phases/04-stable-physics/04-RESEARCH.md
@.planning/phases/04-stable-physics/04-01-SUMMARY.md
@.planning/phases/04-stable-physics/04-03-SUMMARY.md
@.planning/phases/04-stable-physics/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update sim panel with PBF controls and crossfade, un-skip PHYS-09 test</name>
  <files>apollo7/gui/panels/sim_panel.py, apollo7/gui/main_window.py, tests/test_pbf_solver.py, tests/test_sim_panel.py</files>
  <action>
    **Update apollo7/gui/panels/sim_panel.py:**

    Replace old SPH parameter controls with new PBF controls. The simulation panel should have these sliders:

    Essential sliders (visible by default):
    1. **"Cohesion"** (solver_iterations): integer slider 1-6, default 2. Label spectrum: "Ethereal" (1) to "Liquid" (6). This is the user's primary creative control. When changed, use parameter interpolation to crossfade over ~0.5s:
       - Store target value and start time
       - In the panel's update timer (or connect to viewport frame callback), interpolate current solver_iterations toward target using lerp
       - Call engine.update_visual_param("solver_iterations", interpolated_value) each frame during crossfade
       - The PBF solver reads solver_iterations as float and casts to int -- during crossfade, intermediate float values produce the same int (e.g., 2.3 still runs 2 iterations), so the crossfade is about the OTHER params that change with cohesion feel, not literally fractional iterations.
       - Alternative simpler approach: just snap solver_iterations (it's discrete) but crossfade home_strength slightly to smooth the visual transition.

    2. **"Home Strength"** (home_strength): float slider 0.1-20.0, default 5.0. How tightly particles hold their sculptural form.

    3. **"Flow Intensity"** (noise_amplitude): float slider 0.0-5.0, default 1.0. How much curl noise motion.

    4. **"Breathing Rate"** (breathing_rate): float slider 0.05-0.5, default 0.2 (maps to ~5s cycle at 0.2 Hz).

    Advanced sliders (collapsed in a collapsible section):
    5. **"Flow Scale"** (noise_frequency): float 0.05-2.0, default 0.5
    6. **"Swirl"** (vorticity_epsilon): float 0.0-0.1, default 0.01
    7. **"Smoothing"** (xsph_c): float 0.0-0.05, default 0.01
    8. **"Damping"** (damping): float 0.9-1.0, default 0.99
    9. **"Breathing Depth"** (breathing_amplitude): float 0.0-0.3, default 0.15

    Remove old SPH sliders: viscosity, pressure_strength, surface_tension, attraction_strength, repulsion_strength, repulsion_radius, smoothing_radius, gas_constant.

    Each slider should call engine.update_visual_param(param_name, value) on change.

    **Update main_window.py** if needed to wire the updated sim panel to the engine. Check that update_visual_param / update_physics_param connections still work with the new param names.

    **Update tests/test_sim_panel.py** if it exists to reflect new slider names and param ranges.

    **Un-skip test_iteration_count_affects_density (PHYS-09):**
    - Initialize engine with 1000 particles
    - Step 100 frames with solver_iterations=1
    - Read positions, compute spread (std dev of distances from center)
    - Restart, step 100 frames with solver_iterations=4
    - Read positions, compute spread
    - Assert iterations=4 produces tighter spread (lower std dev) than iterations=1
    - This verifies that solver iterations act as a cohesion control
  </action>
  <verify>
    <automated>python -m pytest tests/test_pbf_solver.py tests/test_sim_panel.py -x -q</automated>
  </verify>
  <done>GUI has PBF controls with creative labeling. Solver iterations crossfade smoothly. PHYS-09 test passes. All PBF stubs un-skipped and green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of stable, organic particle physics</name>
  <action>Visual checkpoint -- verify the complete PBF physics pipeline produces the intended aesthetic.</action>
  <what-built>
    Complete PBF physics pipeline: stable solver, GPU spatial hash, home position attraction, curl noise flow fields, vortex confinement, breathing modulation, solver iterations as creative control, and GUI integration.
  </what-built>
  <how-to-verify>
    1. Launch the application: `python -m apollo7`
    2. Load a photo (any JPEG/PNG with reasonable detail)
    3. Observe the particle sculpture for 30 seconds:
       - Particles should form a recognizable shape from the photo
       - Visible slow, sweeping flow like ocean currents (not static, not chaotic)
       - Subtle swirling eddies within the larger flow
       - A gentle breathing cycle (~4-6 seconds) where the sculpture slightly contracts and expands
    4. Adjust the "Cohesion" slider:
       - At 1 (Ethereal): particles should be loose, wispy, gas-like
       - At 2 (default): balanced fluid with visible structure
       - At 4+: dense, tightly held, liquid-like
       - At 6: nearly solid, minimal flow
    5. Adjust "Home Strength": high values = particles snap close to photo shape, low values = more drift
    6. Adjust "Flow Intensity": high = more motion, low = calmer
    7. Verify no crashes, no NaN artifacts (particles snapping to origin), no explosion after 60+ seconds
    8. Verify 60fps (check viewport FPS counter if available, or observe smoothness)
  </how-to-verify>
  <verify>Visual inspection by user</verify>
  <done>User confirms particles exhibit stable, organic, living motion with ocean-current aesthetic.</done>
  <resume-signal>Type "approved" if the sculpture exhibits stable, organic, living motion. Otherwise describe issues.</resume-signal>
</task>

</tasks>

<verification>
python -m pytest tests/ -x -q
</verification>

<success_criteria>
- All 9 PHYS requirements have passing tests (no skipped stubs remain)
- GUI exposes PBF controls with creative labeling
- Solver iterations slider produces visible difference in simulation character
- Visual checkpoint confirms organic, living particle behavior
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/04-stable-physics/04-05-SUMMARY.md`
</output>
