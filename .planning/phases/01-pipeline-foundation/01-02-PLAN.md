---
phase: 01-pipeline-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apollo7/ingestion/__init__.py
  - apollo7/ingestion/loader.py
  - apollo7/ingestion/thumbnailer.py
  - apollo7/ingestion/metadata.py
  - apollo7/gui/panels/__init__.py
  - apollo7/gui/panels/library_panel.py
  - apollo7/gui/panels/controls_panel.py
  - apollo7/gui/widgets/progress_bar.py
  - apollo7/gui/main_window.py
  - apollo7/workers/__init__.py
  - apollo7/workers/ingestion_worker.py
  - tests/test_loader.py
  - tests/test_thumbnailer.py
autonomous: true
requirements:
  - INGEST-01
  - INGEST-02
  - INGEST-03

must_haves:
  truths:
    - "User can load a single photo (JPEG, PNG, TIFF) via browse dialog"
    - "User can batch-ingest a folder of photos with visible progress feedback"
    - "User can see thumbnails and metadata of ingested photos in the library panel"
    - "UI remains responsive during batch ingestion (no freezing)"
  artifacts:
    - path: "apollo7/ingestion/loader.py"
      provides: "Image loading and validation"
      exports: ["load_image", "load_folder"]
    - path: "apollo7/ingestion/thumbnailer.py"
      provides: "Thumbnail generation"
      exports: ["generate_thumbnail"]
    - path: "apollo7/gui/panels/library_panel.py"
      provides: "Photo grid with thumbnails"
      min_lines: 50
    - path: "apollo7/workers/ingestion_worker.py"
      provides: "Background ingestion via QRunnable"
      contains: "QRunnable"
  key_links:
    - from: "apollo7/gui/panels/library_panel.py"
      to: "apollo7/ingestion/loader.py"
      via: "load_image/load_folder calls"
      pattern: "load_image|load_folder"
    - from: "apollo7/workers/ingestion_worker.py"
      to: "apollo7/ingestion/loader.py"
      via: "background loading"
      pattern: "load_image"
    - from: "apollo7/gui/main_window.py"
      to: "apollo7/gui/panels/library_panel.py"
      via: "replacing placeholder panel"
      pattern: "LibraryPanel"
---

<objective>
Implement photo ingestion (single file + batch folder), thumbnail generation, metadata extraction, library panel UI, and background ingestion workers.

Purpose: Users need to load photos into the pipeline before any extraction or rendering can happen. This plan delivers the input side of the pipeline with a polished library panel.

Output: Working photo loading with thumbnails displayed in a grid panel, progress feedback during batch ingestion, and background workers keeping the UI responsive.
</objective>

<execution_context>
@C:/Users/DeveshD/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DeveshD/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-pipeline-foundation/01-RESEARCH.md
@.planning/phases/01-pipeline-foundation/01-CONTEXT.md
@.planning/phases/01-pipeline-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Image loader, thumbnailer, metadata extraction with tests</name>
  <files>
    apollo7/ingestion/__init__.py,
    apollo7/ingestion/loader.py,
    apollo7/ingestion/thumbnailer.py,
    apollo7/ingestion/metadata.py,
    tests/test_loader.py,
    tests/test_thumbnailer.py
  </files>
  <behavior>
    - test_load_single_jpeg: load_image("test.jpg") returns numpy RGB float32 array with correct shape
    - test_load_single_png: load_image("test.png") returns numpy RGB float32 array
    - test_load_unsupported: load_image("test.bmp") raises ValueError with descriptive message
    - test_load_nonexistent: load_image("missing.jpg") raises FileNotFoundError
    - test_load_folder: load_folder(dir_with_3_jpgs) returns list of 3 (path, image) tuples
    - test_load_folder_filters: load_folder(dir_with_mixed_files) only returns supported formats
    - test_generate_thumbnail: generate_thumbnail(image, size=128) returns PIL Image with max dimension 128
    - test_thumbnail_preserves_aspect: wide image thumbnail maintains aspect ratio
    - test_extract_metadata: extract_metadata("test.jpg") returns dict with width, height, format keys
  </behavior>
  <action>
    Create `apollo7/ingestion/loader.py`:
    - `load_image(path: str | Path) -> np.ndarray`: Opens image via Pillow (handles JPEG, PNG, TIFF; use Pillow for format detection via header, not extension). Convert to RGB numpy float32 [0.0-1.0]. Raise ValueError for unsupported formats, FileNotFoundError for missing files.
    - `load_folder(folder: str | Path) -> list[tuple[Path, np.ndarray]]`: Scans folder for supported image files (JPEG, PNG, TIFF extensions: .jpg, .jpeg, .png, .tiff, .tif). Returns list of (path, image_array) tuples. Skips files that fail to load (log warning, don't crash).
    - Supported formats constant: `SUPPORTED_EXTENSIONS = {'.jpg', '.jpeg', '.png', '.tiff', '.tif'}`

    Create `apollo7/ingestion/thumbnailer.py`:
    - `generate_thumbnail(image: np.ndarray, size: int = 128) -> Image`: Takes float32 RGB array, converts to PIL Image, creates thumbnail preserving aspect ratio, returns PIL Image.

    Create `apollo7/ingestion/metadata.py`:
    - `extract_metadata(path: str | Path) -> dict`: Returns dict with keys: width, height, format, file_size_bytes, exif (dict of EXIF data or empty dict if none). Uses Pillow for EXIF extraction.

    Write tests FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>cd "C:/Users/DeveshD/Documents/Claude Projects/Apollo 7" && pytest tests/test_loader.py tests/test_thumbnailer.py -x -v --timeout=30</automated>
  </verify>
  <done>
    - load_image handles JPEG, PNG, TIFF; returns float32 RGB arrays
    - load_folder scans and filters by extension, skips failures
    - Thumbnails preserve aspect ratio
    - Metadata includes dimensions, format, EXIF
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Library panel, controls panel, progress bar, and ingestion worker</name>
  <files>
    apollo7/gui/panels/__init__.py,
    apollo7/gui/panels/library_panel.py,
    apollo7/gui/panels/controls_panel.py,
    apollo7/gui/widgets/progress_bar.py,
    apollo7/gui/main_window.py,
    apollo7/workers/__init__.py,
    apollo7/workers/ingestion_worker.py
  </files>
  <action>
    **Library panel** — Create `apollo7/gui/panels/library_panel.py` with `LibraryPanel(QWidget)`:
    - Grid layout of thumbnail images (use QGridLayout or QFlowLayout). Grid thumbnails suit the thin right panel best — 2 columns of square thumbnails.
    - Each thumbnail is a clickable QLabel with pixmap, bordered, shows filename below
    - "Load Photo" button at top (opens QFileDialog for single file)
    - "Load Folder" button at top (opens QFileDialog for directory)
    - Selected photo emits `photo_selected(path: str)` signal
    - Scrollable via QScrollArea wrapping the grid
    - Shows photo count label: "12 photos loaded"
    - Method `add_photo(path, thumbnail_pixmap, metadata)` to add entries

    **Controls panel** — Create `apollo7/gui/panels/controls_panel.py` with `ControlsPanel(QWidget)`:
    - Placeholder for Phase 1: contains "Extract Features" button and layout mode toggle (Depth-projected / Feature-clustered radio buttons)
    - These controls will be wired to extraction in Plan 03/04
    - Group boxes: "Extraction", "Layout Mode"
    - Styled with the dark theme (inherits QSS from theme.py)

    **Progress bar widget** — Create `apollo7/gui/widgets/progress_bar.py` with `ExtractionProgressBar(QWidget)`:
    - Wraps QProgressBar with label: "Processing 47/200 photos..."
    - Methods: `start(total)`, `update(current, total)`, `finish()`, `hide()`
    - Sits at top of the main window (above the viewport) — only visible during processing
    - Styled via QSS (electric blue chunk on dark background)

    **Ingestion worker** — Create `apollo7/workers/ingestion_worker.py` with `IngestionWorker(QRunnable)`:
    - Takes list of file paths or a folder path
    - Uses WorkerSignals pattern from research: `progress(int, int)`, `photo_loaded(str, object, object)` (path, thumbnail_pixmap, metadata), `finished()`, `error(str)`
    - Calls loader.load_image for each file, generates thumbnail, extracts metadata
    - Emits photo_loaded after each successful photo — enables progressive library population
    - Runs in QThreadPool to keep UI responsive

    **Wire into main window** — Update `apollo7/gui/main_window.py`:
    - Replace right-side placeholder panels with real LibraryPanel and ControlsPanel
    - Add ExtractionProgressBar at top of window (in main vertical layout, above the splitters)
    - Connect LibraryPanel "Load Photo" button to open file dialog, create IngestionWorker, start in QThreadPool
    - Connect LibraryPanel "Load Folder" button to open folder dialog, same worker flow
    - Connect IngestionWorker.photo_loaded signal to LibraryPanel.add_photo
    - Connect IngestionWorker.progress signal to ExtractionProgressBar.update
    - Connect IngestionWorker.finished signal to ExtractionProgressBar.finish

    **IMPORTANT constraints:**
    - Never block main thread — all loading via QThreadPool
    - Library panel must be scrollable for many photos
    - Thumbnail generation happens in worker thread, not main thread
    - Convert PIL thumbnails to QPixmap in main thread (Qt requires main thread for pixmap creation)
  </action>
  <verify>
    <automated>cd "C:/Users/DeveshD/Documents/Claude Projects/Apollo 7" && pytest tests/ -x -q --timeout=30</automated>
  </verify>
  <done>
    - Library panel shows grid of thumbnails for loaded photos
    - "Load Photo" opens file dialog, loads photo, shows thumbnail in library
    - "Load Folder" opens folder dialog, batch-loads with progress bar visible
    - Progress bar shows "Processing X/Y photos..." during batch loading
    - UI stays responsive during loading (no freezing)
    - Controls panel shows placeholder extraction button and layout mode toggle
    - All existing tests still pass
  </done>
</task>

</tasks>

<verification>
- `pytest tests/ -x -q --timeout=30` passes (including new loader/thumbnailer tests)
- Load a single JPEG via browse dialog — thumbnail appears in library panel
- Load a folder of photos — progress bar shows, thumbnails appear progressively
- UI remains interactive during batch loading
</verification>

<success_criteria>
- Single photo and batch folder loading works end-to-end
- Library panel displays thumbnails in a scrollable grid
- Progress feedback visible during batch operations
- Background workers keep UI responsive
- Loader tests cover happy path and error cases
</success_criteria>

<output>
After completion, create `.planning/phases/01-pipeline-foundation/01-02-SUMMARY.md`
</output>
